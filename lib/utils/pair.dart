/// Create an immutable pair of any two class instances.
/// Much like `Pair` in Kotlin or a 2-tuple in Python.
class Pair<T1, T2> {
  final T1 first;
  final T2 second;

  Pair(this.first, this.second);
}